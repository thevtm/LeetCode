export {}; // Necessary in order to avoid TS errors

interface ListNode {
  val: number;
  next: ListNode | null;
}

function isPalindrome(head: ListNode | null): boolean {
  const middle = null;

  let slow = head;
  let fast = head;
}
